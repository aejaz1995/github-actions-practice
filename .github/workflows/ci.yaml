name: My First CI

on:
  push:

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello from GitHub Actions 🚀"

      - name: Show Node version
        run: node -v
